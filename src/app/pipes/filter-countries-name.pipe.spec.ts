import { FilterCountriesNamePipe } from './filter-countries-name.pipe';

describe('FilterCountriesNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCountriesNamePipe();
    expect(pipe).toBeTruthy();
  });
});
