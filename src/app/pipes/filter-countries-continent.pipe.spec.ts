import { FilterCountriesContinentPipe } from './filter-countries-continent.pipe';

describe('FilterCountriesContinentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCountriesContinentPipe();
    expect(pipe).toBeTruthy();
  });
});
